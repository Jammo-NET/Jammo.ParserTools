<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>netcoreapp3.1</TargetFramework>
        <Description>Tools I use frequently for text parsing</Description>
        <RepositoryUrl>https://github.com/Jammo-NET/Jammo.ParserTools</RepositoryUrl>
        <RepositoryType>git</RepositoryType>
        <PackageReleaseNotes>MAJOR CHANGE

EnumerableNavigator now starts at the index -1 instead of 0. In the old model, the user was forced to use a do..while loop to encapsulate every item.

Add EnumerableIterator, a lightweight version of EnumerableNavigator that is moreso a wrapper for IEnumerators</PackageReleaseNotes>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <Title>Jammo.ParserTools</Title>
        <Authors>zeplar exe</Authors>
        <PackageLicenseUrl>https://github.com/Jammo-NET/Jammo.ParserTools/blob/master/LICENSE</PackageLicenseUrl>
        <PackageVersion>1.4.0</PackageVersion>
        <LangVersion>9</LangVersion>
    </PropertyGroup>

</Project>
